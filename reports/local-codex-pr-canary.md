# Local Codex PR Canary

This file is a temporary canary for verifying the local Codex worker branch -> push -> PR lane on `agent-asset-marketplace`.

It should be short-lived and removed after the workflow is confirmed.
