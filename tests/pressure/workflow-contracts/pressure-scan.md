# MARK-373 pressure scan classification

Raw scan: `pressure-scan.json` (99 candidate hits; generated by
`tools/workflow_pressure_scan.py`). Scanner output is candidate evidence, not
an automatic failure.

| candidate family | count | classification | owner/reason |
|---|---:|---|---|
| `portable-repo-command` | 87 | `repo-local` / `deferred` | Repo runbooks and repo-worker standards may name their own commands; portable Superpowers references require Task 3/4 review and repair. |
| `full-test-suite` | 5 | `intended` / `deferred` | Test and completion gates may require a broad final proof; Task 4/6 removes ritual repetition and caller strengthening. |
| `must-read` | 5 | `intended` / `deferred` | Narrow safety/reference gates are intentional; Task 3 checks for eager broad loading. |
| `every-function` | 1 | `deferred` | Task 4 owns proportionate TDD and transitive coverage. |
| `universal-startup-read` | 1 | `deferred` | Task 3 owns classify-before-bootstrap and bounded reading. |

No raw hit is treated as a defect at this staged point. The deferred rows are
owned by Tasks 3–6; the final classification must contain no unresolved
`defect` before the Task 6 evaluation checkpoint.
