version: 1
metadata:
  skill_name: agent-evaluation
  source_category: first_party
interface:
  display_name: Agent Evaluation
  short_description: Use when evaluating agent outputs, designing benchmarks, or building self-evaluation rubrics.
  default_prompt: Use agent-evaluation when evaluating agent outputs, designing benchmarks, or building self-evaluation rubrics.
policy:
  allow_implicit_invocation: true
