# Source

This plugin projects the first-party agent-operating-model skills.

## Boundary
- Canonical sources live in this plugin's `skills/` tree.
- The bundle owns repository operating-model capabilities, while
  `repo-worker-pack` owns portable worker execution and source custody.
- Framework-specific Python guidance remains in `language-patterns-pack`.
