version: 1
metadata:
  skill_name: release-engineering
  source_category: first_party
  plugin: planning-pack
  projection_plugin: planning-pack
interface:
  display_name: Release Engineering
  short_description: Use when building, reviewing, or operating CI/CD pipelines, container images, releases, rollbacks, or deployment patterns.
  default_prompt: Use release-engineering when building, reviewing, or operating CI/CD pipelines, container images, releases, rollbacks, or deployment patterns.
policy:
  allow_implicit_invocation: true
