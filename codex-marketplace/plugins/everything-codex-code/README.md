# Everything Codex Code

This mega pack projects the full selected ECC slice into one reference bundle
for marketplace inspection.

## Bundle contents
See `references/bundle-manifest.json` for the selected set.

## Documentation
- provenance and source mapping in `SOURCE.md`
- bundle inventory in `references/bundle-manifest.json`
- canonical source mapping in `references/source-map.md`

## Boundary
- The mega pack is a reference bundle for the selected ECC slice.
- The topical packs remain the primary homes for day-to-day usage.

## Install shape

The installable skill zips are generated under
`generated/skill-zips/<skill-name>.zip` and can be
installed directly from those artifacts.
