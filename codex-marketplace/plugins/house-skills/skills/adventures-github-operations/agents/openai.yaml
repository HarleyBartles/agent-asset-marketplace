interface:
  display_name: Adventures GitHub Operations
  short_description: Verify Adventures repo, asset, deck, receipt, and issue evidence.
  default_prompt: Use /adventures-github-operations when use adventures github proof
    work for branch, pr, issue, and evidence handling after the generic `github-operations`
    proof skill has already handled review routing; keep it focused on repository
    proof surfaces and avoid visual or creative authority.
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
