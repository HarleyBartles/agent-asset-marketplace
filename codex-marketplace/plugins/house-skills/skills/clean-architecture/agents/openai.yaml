version: 1
metadata:
  skill_name: clean-architecture
  plugin: house-skills
  source_category: first_party
  projection_plugin: house-skills
interface:
  display_name: Clean Architecture
  short_description: Use when designing testable, framework-independent applications with clear dependency rules. Do not use when the team is committed to a framework-centric stack and the cost of ports/adapters is unjustified.
  default_prompt: Use when designing testable, framework-independent applications with clear dependency rules. Do not use when the team is committed to a framework-centric stack and the cost of ports/adapters is unjustified.
policy:
  allow_implicit_invocation: true
