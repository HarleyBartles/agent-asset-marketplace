interface:
  display_name: Adventures Image QA
  short_description: QA Adventures generated images across scene, asset, and reference
    lanes.
  default_prompt: Use /adventures-image-qa when review adventures generated candidates
    and compiled assets against source constraints and acceptance criteria, while
    keeping qa separate from image generation or editing authority.
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
