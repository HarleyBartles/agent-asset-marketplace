version: 1
metadata:
  skill_name: ddd
  plugin: house-skills
  source_category: first_party
  projection_plugin: house-skills
interface:
  display_name: DDD
  short_description: Use when modeling a complex business domain, defining bounded contexts, or choosing tactical DDD patterns.
  default_prompt: Use when modeling a complex business domain, defining bounded contexts, or choosing tactical DDD patterns.
policy:
  allow_implicit_invocation: true
