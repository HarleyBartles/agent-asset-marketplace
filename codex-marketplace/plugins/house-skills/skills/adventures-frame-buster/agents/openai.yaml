version: 1
interface:
  display_name: Adventures Frame Buster
  short_description: Routes Adventures framing checks and readiness gates.
  default_prompt: Use /adventures-frame-buster when route adventures frame-readiness
    checks as deterministic source-bound work before storyboard, preproduction, or
    package production, without invoking image-generation authority.
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
