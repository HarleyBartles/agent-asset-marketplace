version: 1
metadata:
  skill_name: database-design-patterns
  source_category: first_party
  plugin: house-skills
  projection_plugin: house-skills
interface:
  display_name: Database Design Patterns
  short_description: Use when designing relational database schemas, normalizing data, choosing keys and constraints, or applying transactions, indexing, and partitioning patterns.
  default_prompt: Use when designing relational database schemas, normalizing data, choosing keys and constraints, or applying transactions, indexing, and partitioning patterns.
policy:
  allow_implicit_invocation: true
