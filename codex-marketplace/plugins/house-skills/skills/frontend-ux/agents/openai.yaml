version: 1
metadata:
  skill_name: frontend-ux
  plugin: house-skills
  source_category: first_party
  projection_plugin: house-skills
interface:
  display_name: Frontend UX
  short_description: Use when designing, reviewing, or debugging frontend user interfaces.
  default_prompt: Use frontend-ux when designing, reviewing, or debugging frontend user interfaces.
policy:
  allow_implicit_invocation: true
