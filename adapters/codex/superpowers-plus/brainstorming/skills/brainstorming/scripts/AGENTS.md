Use the `.sh` bash scripts by default when the environment has bash.
If bash is unavailable, use the sibling `*.ps1` script with the same basename.
Keep the script's behavior and output contract aligned across both versions.
