# AGENTS.md

STOP. This is a historical directory. Do not create or maintain files here.

Durable Superpowers plans, specs, and doctrine now live in `.agents/plans/`
and `.agents/specs/`. SDD output and session artifacts go in the off-repo
scratch path returned by `sdd-workspace`.
