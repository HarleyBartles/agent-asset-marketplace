# Trustworthy Iterative Review - Epic Roadmap

Source spec: [Trustworthy Iterative Review Design](../../specs/2026-08-21-trustworthy-iterative-review-design.md)

| # | Title | Status | Plan File | Commit | PR | Rating | Notes |
|---|---|---|---|---|---|---|---|
| 1 | Trust-root capability gate, fail-closed evidence kernel, and legacy false-green regression suite | ready | [Plan 1](2026-08-21-plan-1-evidence-kernel.md) | - | - | 9/10 | First prove external receipt custody/retrieval, blind-tool confinement, and sealed-byte command isolation, then establish the sole strict machine state, exhaustive projections, sealed policies, immutable evidence, locked transactions, typed lifecycle, and false-green regressions. |
| 2 | Immutable snapshot epochs and authority manifest | pending | - | - | - | - | Bind full base/head/tree identifiers, diff and metadata hashes, repo law, linked issue documents, and drift invalidation to each review epoch. |
| 3 | Impact inventory, coverage planner, and safe reviewer packages | pending | - | - | - | - | Replace heuristic lens selection with changed/affected-surface obligations, derive scope-plus-risk capability floors, require risk-based overlap, and build context packages that cannot hide dependencies. |
| 4 | Structured runtime execution and attestation validation | pending | - | - | - | - | Verify confined immutable-source command and profile-resolution/launch/completion receipts, dispatch intentionally tiered roles under sealed role floors/independence, require role-specific custom-profile qualification, and require positive evidence for every assignment. |
| 5 | Finding adjudication, candidate-fix impact, and same-snapshot review-repair lifecycle | pending | - | - | - | - | Track all severities, independently adjudicate remediation class, enforce fixed/review-repaired/false-positive proof, and re-ascend every invalidated review tier before final review. |
| 6 | Trusted final-strong blind final, closure audit, exact-SHA seal, and atomic presentation | pending | - | - | - | - | Re-resolve final/closure routes, implement ready transition, bind full hosted workflow/trigger/input identities, and make presentation race-safe against concurrent state mutations. |
| 7 | Frontier-reference benchmark, escape measurement, simplification, migration, and rollout | pending | - | - | - | - | Gate weaker routes against a frontier-stable corpus, measure downstream frontier escapes, cut over to one `reviewctl` path, remove bookkeeping nodes and legacy authorities, and publish consumer-safe guidance. |

## Epic invariants

- No plan may weaken the green predicate in the source spec.
- Each plan uses test-driven development and lands as a reviewable draft pull request before the next plan is written.
- Version-1 state, reports, and metrics may inform migration diagnostics but may not satisfy a version-2 green gate.
- A known `accepted-risk` finding may produce `reviewed-with-exceptions` but never green.
- A stored seal is only a candidate proof; human-facing green requires a fresh remote head, authority, and hosted-check revalidation.
- Review dispatches follow a monotonic scope-to-reasoning ladder: broader aperture or greater consequence raises the minimum capability and reasoning floor; a lower tier may discover findings but may never satisfy a higher-tier obligation.
- A sealed review-assignment policy supplies non-null role floors and independence rules before obligation floors exist: mappers are at least strong/fresh/distinct, the scope challenger is final-strong/fresh/distinct, and adjudication/repair verification inherit source and linked-assignment floors and cannot validate their own source work.
- The blind final reviewer and closure auditor run in fresh independent contexts through an effective harness-defined custom profile such as `reviewer-strong` only when a trusted authority outside the reviewed head qualifies that exact semantic role. Baked model/reasoning is trusted and not overridden; wrong-role or anchored profiles, self-qualification, missing runtime receipts, unresolved routes, and silent downgrade block.
- Local state, check/report JSON, and hashes prove consistency but not execution provenance. Authority discovery, command execution, profile resolution, reviewer launch/completion, and remote observations require independently retrievable trusted-adapter receipts selected by a sealed composition-root trust policy. Candidate commands run from the exact sealed tree in a disposable sandbox with no review-authority/evidence access and complete process-tree teardown; caller-selected issuers/locators, mutable source, reused launch challenges, or unavailable receipt/confinement support block.
- Every mutation holds an exclusive state lock across load, ingestion, validation, history, and compare-and-swap replacement. Immutable content identity is separate from epoch-bound evidence identity, so concurrent findings cannot be lost and unchanged bytes can remain historically bound across replacement epochs.
- Cutover requires both deterministic state-soundness tests and 100% per-trial recall of the versioned frontier-reference issue set for every supported weaker-model/profile combination.
- A valid in-scope issue first found by an independent external frontier reviewer after internal green is a frontier escape: the benchmark pass is revoked, the missed tier is classified, and the case becomes a permanent regression fixture.
- Source edits remain under `codex-marketplace/plugins/superpowers-plus/skills/iterative-review/` and `codex-marketplace/plugins/superpowers-plus/skills/selecting-a-subagent/`; `.agents/skills/` remains generated.
- Consumer repositories supply local commands and domain obligations. Portable source must not assume this repository's paths outside explicit repo-local adapters.
- Until Plan 7 cuts over and passes the benchmark plus pressure campaign, the existing skill must be described as review assistance, not proof of reviewed green.
- Until that cutover, the legacy skill is non-implicit and human-opt-in for each PR. Harness-designated frontier orchestrators, including `gpt-5.6-sol`, bypass it in favor of ordinary whole-change self-review plus canonical validation; its terminal `ready` value cannot authorize a green claim or draft-to-ready transition.
- Synthetic adapters may test kernel logic only after a pre-Plan-1 live capability gate proves that every production receipt/tool-confinement route exists outside candidate control. Missing custody or retrieval blocks implementation instead of being abstracted away.
- All JSON ingress rejects duplicate keys before canonicalization; receipt verification binds full review/dispatch/epoch/fingerprint scope, not only a subject digest.
- Every record and receipt target has an exhaustive constructor-owned projection, derived/external ID rule, and independent canonical-byte/hash golden vector; constructor/validator self-consistency is not acceptance proof.

## Plan acceptance boundaries

### Plan 1 - Evidence kernel

- A published pre-kernel capability matrix proves external issuer/locator custody, independent retrieval, one-time execution identity, exact raw-byte preservation, blind-tool confinement, and immutable-source/process-tree command isolation for authority discovery, profile resolution, review launch/completion, command execution, ready transition, remote observation, and tool results. Failure stops before the synthetic kernel.
- Strict version-2 state, exhaustive non-circular subject/receipt projections with independent golden vectors, impact/challenged-coverage and canonical hypothesis records, typed consequence and role floors, structured exemption and review-repair proof, sealed receipt/local-check/review-assignment/command-execution/evidence-ingestion/hypothesis policies, duplicate-key rejection, review-owned immutable content plus epoch evidence bindings, cross-reference validation, chained history, locked compare-and-swap writes, discriminated feedback/review/check finding lifecycles, reserved ready-to-CI stage, monotonic re-ascent, and transient green evaluation exist behind one CLI. Plan 1 validates synthetic adapter outputs only after the capability gate and blocks live receipt-dependent actions without trusted adapters; Plans 2-6 supply live acquisition/execution adapters.
- Every known version-1 false-green/dead-route defect is a fixture proving legacy state cannot satisfy version 2; only the current baseline routing-test inconsistency is repaired.
- Missing one green predicate, incomplete authority manifest, unverified/fabricated receipt, skipped or downgraded tier, wrong-role/overridden final profile, unqualified fallback, unsafe evidence source, unsupported exemption, stale bytes, persisted green, accepted risk, and malformed remote observation all fail closed.

### Plan 2 - Snapshot and authority

- A sealed versioned discovery policy rooted in an external or base authority—not the reviewed head—defines repository/directory-law and PR roots, typed authority edges, recursive fixed-point traversal, canonical deduplication, and ambiguity behavior. Trusted connector discovery materializes full repository/PR/base/head/tree/diff/metadata identity, typed feedback, all sealed-policy identities/digests, and loaded/unavailable authority records into one canonical manifest payload. Its independently retrievable receipt binds the complete snapshot subject/fingerprint, discovery-policy source/version/digest, and typed manifest digest; omission and self-qualification fixtures cover every root/edge.
- A sealed provider-history policy enumerates all actionable threads/change requests, including items resolved before initial freeze. Each creates a feedback authority and immutable provider/thread-keyed finding unless already closed by this review lifecycle. Provider Resolve changes the unresolved subset but cannot erase the finding; fixed/false-positive proof remains required. Live omission fixtures include resolve-before-freeze and resolve-between-refresh cases.
- Base/head/tree, content-bearing PR scope/metadata, governing authority, actionable feedback-history/resolution-state changes, discovery/feedback/receipt policy, or required-check-policy drift uses trusted `refresh-review-input`, creates a new epoch, and invalidates current evidence according to explicit rules. Lifecycle-only draft/ready state is excluded from the content projection and receipt-bound separately.
- Missing connectors, unavailable linked authority, ambiguous base, shallow history, uncommitted review input, or non-remote head blocks.

### Plan 3 - Impact and coverage

- Independent semantic dependency mapping and contract/data-flow/user-journey mapping produce a union of changed and affected surfaces; a separate challenger attacks omissions.
- The two mappers run at least strong/high in fresh execution-distinct role contracts; the challenger runs final-strong/final-strong in a fresh execution and role contract distinct from both. Missing qualified independence blocks before obligations can be defined from a weak universe.
- Every affected surface receives universal and repo/domain obligations. Maps/inventory carry a closed consequence vocabulary, and each obligation records its aperture and the deterministic maximum of scope, risk, and typed consequence floors. A sealed policy derives first-class epoch-namespaced complementary hypothesis records whose IDs are dispatchable and seal-bound. High-risk work gets opposite-polarity hypotheses and two distinct-contract strong reviews; `not-applicable` requires concrete counter-evidence plus receipt-verified review at the same floor, and high risk additionally requires a distinct-contract final-strong exemption challenger. Zero lens matches or unavailable independence blocks.
- Reviewer packages contain the complete patch, authorities, dependency context, generated-source provenance, and read-only repo access. Diff slices are navigation only and cannot hide context.

### Plan 4 - Reviewer execution

- Wire trusted harness adapters for deterministic/targeted command execution and reviewer dispatch. Commands start from a runner-materialized immutable lower tree and execute in a disposable confined sandbox with hashed executable/interpreter/script/module/toolchain/environment identity, no access to review state/evidence/authority/credentials or unapproved network, writable policy mounts only, equal pre/post source digests, and complete job/cgroup teardown. Receipts bind every field; dirty-checkout substitution, surviving children, and copied success JSON cannot satisfy a check.
- Preserve intentionally different `reviewer-fast`, `reviewer-fixes`, `reviewer`, and `reviewer-strong` roles, but make their policy tiers explicit: hunk/file mechanical review may use `fast`; bounded fixes and surfaces use at least `focused`; cross-file, security, architectural, and high-risk review uses at least `strong`; whole-PR synthesis uses `final-strong`.
- Enforce the sealed review-assignment role table before route selection. Mapper/challenger/adjudicator/repair-verifier actions use fresh receipt-distinct executions; adjudicators inherit the maximum source/linked-obligation floor and cannot reuse the source review's execution/role contract.
- Resolve each semantic role through `selecting-a-subagent` and a trusted harness authority outside the reviewed head. If an effective custom profile such as `reviewer-strong` satisfies that exact role, dispatch it by name without model/reasoning overrides and trust its baked route. A strength-qualified synthesis profile that consumes prior logs cannot satisfy blind final. Record readable profile bytes or an opaque adapter identity, plus a profile-resolution receipt.
- When no named custom profile exists, use the current runtime role mapping; only then fall back to qualified inheritance or an explicit route. Record the qualification source and reject absent profiles, mismatched role identity, profile overrides, or fallbacks below the assignment floor.
- Reviewer profiles are role-bounded and portable, with explicit assignment IDs, model/context/profile identity, structured JSON output, inspected surfaces, tested hypotheses, commands, findings, and uncertainties. Canonical Devin, Codex, and shared policy surfaces encode the same ladder without assuming provider names, prices, entitlements, or that every parent is the strongest available route.
- Role-discriminated outputs carry machine outcomes and canonical structured-artifact digests. Mapper/challenger records are derived from exact receipt-bound map/inventory/obligation bytes; adjudication and fix/exemption results cannot be interpreted from prose.
- Supply role-specific `blind-final` and `closure-auditor` contracts (either dedicated profiles or explicit trusted modes). The current vendored `reviewer-strong` contract consumes prior lens logs and is therefore ineligible for blind final until replaced or adapted; a harness-owned custom `reviewer-strong` may be trusted when its independently resolved contract already meets the role.
- Dispatcher validation independently re-fetches profile-resolution, launch, and completion receipts through the sealed per-kind issuer registry and compares kind/review/dispatch/epoch/fingerprint/subject/challenge scope. Launches consume post-registration single-use challenges and prove the realized profile/role/model/reasoning/context, instruction/data channels, exact injected bytes, and immutable route token. Completion binds exact raw report bytes and the ordered typed tool transcript. Duplicate keys, mapping races, caller-fabricated provenance, cross-review replay, wrong scope/issuer/namespace/profile/role/route, omitted assignments, structured-output substitution, malformed reports, unsupported outcomes, silent downgrade, and partial/tool-capped runs block.
- Blind-final context is a constructor-owned split between trusted instructions and inert raw snapshot/diff/authority data. Runtime reads use a snapshot-pinned role proxy that denies review state, prior reports/findings, and feedback/comments; every returned digest is transcript-bound. Free-form orchestrator context, hazard framing, copied conclusions, and instruction-channel injection are forbidden. Final and closure require affirmative `covered`, never `not-applicable`. Independent exemption review uses distinct role/profile-contract authority and canonical policy-derived hypotheses, not raw digest inequality.

### Plan 5 - Findings and fixes

- Every severity follows an immutable lifecycle. Only receipt/evidence-backed `fixed`, `review-repaired`, or `false-positive` satisfies green; `fixing` and `review-repairing` remain unresolved, `accepted-risk` produces `reviewed-with-exceptions`, and all other dispositions block.
- Every finding-producing gate interrupts immediately. Explicit receipt-verified adjudication emits a strict per-finding outcome plus `candidate-change` or `review-process` remediation class. Candidate change permits published `enter-fixing` or accepted risk. Review process permits same-snapshot `enter-review-repair` or accepted risk; the engine invalidates the typed target/downstream cut, reruns the earliest gate, requires independent current repair verification, closes only from existing proof, then reruns blind final and closure. No-op commits, byte-identical refresh, self-verification, and cross-branch reuse are rejected.
- No cumulative historical count, stale regression link, overwritten origin field, missing round increment, circular resolution artifact, or arbitrary round cap can discard an unresolved issue or authorize green.

### Plan 6 - Independent closure and remote presentation

- Planning/archive/closeout mutations happen before the final freeze. A blind final reviewer and separate closure auditor both produce current receipt-verified attestations from fresh contexts through separately re-resolved role-specific final-strong contracts; a generic anchored `reviewer-strong`, disagreement, uncertainty, receipt/profile dispatch failure, unqualified fallback, or downgrade blocks.
- Required local checks are the exact complete set from a sealed external/base consumer policy; substituted harmless commands, wrong working directories, or omissions cannot count. Required hosted checks must succeed with policy item, app, workflow ID/path/definition ref/SHA, authorized event/trigger subject, policy inputs/configuration, check-run, workflow-run, run-attempt, and head identity; superseded attempts cannot count. Live provider fixtures cover multiple attempts and unauthorized manual triggers. Failures atomically materialize findings. The reserved `mark-ready-for-ci` action durably registers intent/idempotency, then challenge, then independently verified remote result; crash recovery and initially-ready no-op are explicit, and completion creates a CI candidate, not green.
- A candidate seal is content-bound. `reviewctl present` captures generation and exact state-byte digest, obtains a trusted fresh full remote observation, then re-locks and compares state immediately before evaluating and emitting a proof bound to both. A concurrent mutation retries or blocks. Remote evidence includes repository/PR/head, authority/feedback, full check-policy/workflow-definition/trigger/input identity, and exact current attempts; no stale generation or race can emit green.

### Plan 7 - Benchmark and cutover

- A versioned mutation/PR corpus spans every universal category. The reference set contains findings independently reproduced by two blinded frontier runs or confirmed by human adjudication.
- Every supported weaker-model/profile combination achieves 100% reference-finding recall on every one of at least three fresh trials per case, with zero false greens. New frontier-stable misses become permanent fixtures.
- Benchmark and dogfood PRs receive an additional blinded external frontier audit after internal green. The release target is zero frontier escapes across benchmark trials; production reports escape count per green PR by severity, obligation category, and tier that should have caught it, without presenting zero future escapes as a correctness guarantee.
- Adversarial pressure tests cover truncation, duplicate JSON keys, no matching lens, stale/wrong-trigger/wrong-definition CI, post-final and concurrent-presentation drift, provider-resolved feedback, anchoring through runtime tool reads or instruction injection, premature-green pressure, generated-source omissions, cross-lens fixes, unavailable tools/context/receipts, cross-review replay, fabricated receipts, repo-local policy/profile self-qualification, unsafe evidence paths, dirty/substituted/unconfined local commands and surviving children, below-floor mapper/challenger/self-adjudication, same-snapshot process-repair deadlocks, canonical-projection field omission, unsupported `not-applicable`, missing canonical hypotheses/consequences, role-mismatched baked/opaque profiles, forbidden overrides, and profile/mapping drift.
- `SKILL.md` becomes a sub-500-word control plane; metrics, encoding normalization, ledger rendering, and summaries are derived CLI views rather than semantic graph nodes; version-1 authorities and parallel routes are removed; generated marketplace surfaces and consumer migration guidance are verified.

## Epic done gate

The epic is done only when all seven plans are merged, canonical staged CI and exact-SHA hosted CI pass, the Plan 7 benchmark has 100% per-trial reference recall, zero false greens, and zero frontier escapes, the marketplace/generated copies match canonical source, a fresh end-to-end dogfood review produces a presentation proof, and no user-facing path can label version-1 or incomplete version-2 work reviewed green.

## Assessment traceability

| Proven weakness | Owning plan and closure evidence |
|---|---|
| Router sequences names without proving evidence; final review can be skipped or unvalidated | Plans 1 and 4: pure predicates, dispatch/report matching, remove-one-predicate tests |
| Circular resolution entry, cumulative preflight counts, never-closing regressions, lost normalization origin, missing round advancement | Plans 1 and 5: frozen legacy fixtures, replacement lifecycle tests; no repair investment in the dead graph beyond current baseline CI |
| `blocked` cannot lawfully resume | Plan 1: blocker/resolution evidence and transition-table tests |
| Stale initial diff/head/authority after fixes or closeout mutation | Plans 2, 5, and 6: epochs, fix invalidation, closeout-before-freeze, exact-SHA presentation |
| Heuristic lens selection can produce zero deep review and misses affected dependencies | Plan 3: dual impact maps, universal obligations, non-zero assignment gate, independent scope challenge |
| Diff slicing hides context | Plan 3: complete patch plus dependency/context package and live read-only repo access |
| Reviewer profiles are narrow, speed-biased, anchored, or silently truncated by tool caps | Plans 4 and 7: bounded role profiles, blind context exclusion, incomplete/redispatch semantics, weaker-model pressure trials |
| The graph names strong profiles but does not force increasing reasoning as review aperture widens, and pre-obligation mapper/challenger/adjudicator roles have no inherited floor | Plans 1, 3, 4, 5, and 6: sealed portable role floors/independence, scope/risk/source floors in state, monotonic tier policy, trusted-profile qualification, post-fix/repair re-ascent, and final-strong challenger/final/closure tests |
| The workflow can ignore or override a custom subagent whose model is intentionally baked into its harness profile | Plans 1 and 4: custom-profile-first resolution, effective profile/adapter identity evidence, no-override tests, and qualified fallback only when the named profile is absent |
| No structured coverage attestation, finding proof, or dispatch identity | Plans 1, 4, and 5: strict records, schema validation, all-severity lifecycle and evidence hashes |
| Caller-authored check/report/route JSON can impersonate executed work | Plans 1 and 4: trusted receipt-verifier protocol plus harness-minted command/profile-resolution/launch/completion receipts bound to exact command/context/output digests and sealed runner confinement identity |
| Receipt/manifest/evidence hashes can be self-referential, omit semantic fields, disagree across implementations, or overwrite byte-identical history across epochs | Plan 1: exhaustive per-record/receipt projection table, independent canonical-byte/hash golden vectors and mutation tests, ordered construction, immutable content IDs, and distinct epoch evidence bindings |
| Untrusted state can redirect a generic receipt fetcher or replay a prior launch | Plans 1 and 4: sealed per-kind issuer/locator policy, post-registration single-use launch challenges, execution-identity uniqueness, and replay tests |
| Atomic rename can lose a concurrently recorded finding through stale whole-state replacement | Plan 1: exclusive lock across the entire mutation and generation plus exact-prior-byte compare-and-swap tests |
| A profile under review can self-qualify, or a strong anchored synthesis profile can masquerade as blind final | Plans 1, 4, and 6: external profile authority, role-specific capability contracts, per-launch final/closure re-resolution, and launch-manifest exclusion proof |
| Authority completeness can validate against an omitted caller-supplied set | Plans 1 and 2: strict expected-authority manifest plus trusted connector-discovery receipt and feedback/check-policy bindings |
| A connector can faithfully sign an incomplete traversal, or unreadable authority cannot be represented | Plan 1: generic verifier-failure blocking and discriminated unavailable records; Plan 2: live sealed root/edge traversal with real per-edge omission fixtures |
| Stable unresolved review feedback can remain open while all modeled findings close | Plans 1, 2, and 6: typed feedback authorities, canonical empty-set green gate, and exact presentation re-fetch |
| Provider-side Resolve before or during review can erase blocking feedback without a finding disposition | Plans 1, 2, and 5: sealed full-history enumeration plus immutable provider/thread-keyed findings whose closure is independent of current provider state |
| `not-applicable` can erase obligations with arbitrary evidence and create vacuous tiers | Plans 1 and 3: structured assignment attestations at the obligation floor and independent high-risk exemption overlap |
| Relabeled clones can masquerade as independent high-risk exemption reviewers | Plans 1, 3, and 4: distinct launch/completion/run/context identities, roles, and trusted profile/contract authorities |
| Superficially different hashes can masquerade as semantically independent exemption review | Plans 1, 3, and 4: policy-derived orthogonal hypotheses plus a distinct-contract final-strong exemption challenger |
| Hypothesis IDs and consequence floors are required but not machine-representable | Plans 1 and 3: first-class canonical hypothesis records plus closed typed consequences propagated map-to-inventory-to-obligation |
| Evidence hashing can touch arbitrary/UNC/reparse paths or race mutable sources | Plan 1: allowlisted one-handle bounded ingestion into a review-owned immutable content store; verification reads only owned bytes |
| Local checks or old hosted CI can be mistaken for current PR proof | Plans 2 and 6: full snapshot identity, required-check discovery, exact remote SHA, transient presentation observation |
| A trusted runner can execute a harmless substituted/dirty-checkout command, expose review authority/evidence, or leave a background mutator while preserving HEAD | Plans 1 and 4: sealed local-check plus command-execution policies, immutable source materialization, toolchain/environment/confinement identity, denied authority/evidence access, pre/post source equality, full process-tree teardown, and hostile-command fixtures |
| A hosted check can be dressed as a local command result, or a failing check can avoid finding intake | Plans 1 and 6: discriminated check provenance and atomic check-sourced finding materialization |
| CI rerun attempts share a workflow run ID and can select stale success | Plan 1: generic untrusted-current-attempt failure blocks; Plan 6: live provider multi-attempt selection and superseded-attempt fixtures |
| Name-only CI and an unbound PR URL can accept the wrong repository, workflow, app, run, trigger, definition, inputs, or feedback state | Plans 1, 2, and 6: canonical repository/PR identity, typed feedback/check policy, and workflow-definition/event/trigger/input/attempt/head-bound observations |
| Accepted or deferred known defects can be called green | Plans 1 and 5: only fixed/review-repaired/false-positive is green; accepted risk has a distinct non-green terminal result |
| A confirmed closure/review-process defect on unchanged candidate bytes has no lawful remediation path | Plans 1 and 5: typed review-process adjudication, same-snapshot target/downstream invalidation, independent repair verification, review-repaired closure, and mandatory fresh final/closure regression |
| One catch-all finding-resolution payload deadlocks non-fix branches and can fabricate proof during closure | Plans 1 and 5: distinct adjudicate, false-positive, enter-fixing, targeted-check, fix-review, fixed-close, enter/verify/close review-repair, and accepted-risk actions consuming pre-existing proof |
| Generic adjudicator prose cannot machine-authorize a resolution branch | Plans 1 and 5: strict per-finding confirmed/false-positive/contested outcomes bound to evidence and branch compatibility tests |
| Accepted-risk exists but no adjudication outcome can reach it | Plans 1 and 5: confirmed authorizes exactly one fix-or-human-accept branch; acceptance atomically terminates non-green |
| Mapper/challenger receipts can be paired with substituted structured records | Plans 1, 3, and 4: receipt-bound subject digests and derivation of maps/inventory/obligations from exact matching bytes |
| Non-fix remote/authority drift has no lawful replacement-epoch path | Plans 1, 2, and 6: trusted `refresh-review-input` transition with full invalidation and re-ascent |
| Final context can copy prior conclusions under new evidence IDs | Plans 1, 4, and 6: constructor-owned raw-subject allowlist and launch-attested injected bytes |
| Blind-final tools or instruction channels can recover prior conclusions after launch | Plans 1, 4, and 6: split instruction/inert-data manifests, snapshot-pinned role proxy, denied review namespaces, and receipt-bound typed tool transcript |
| Named-profile mapping can change between qualification and launch | Plans 1 and 4: immutable resolved-route token plus launch receipt for realized configuration |
| Closed blockers can lack resolution proof or multiple blockers make resume ambiguous | Plan 1: exactly one active blocker and explicit blocker-ID/current-resolution-evidence transition |
| Draft-to-ready lifecycle metadata can spuriously invalidate content review | Plans 2 and 6: explicit PR-metadata projection and separately receipt-bound ready transition |
| Version-2 kernel omits its normative draft-to-ready stage/action | Plans 1 and 6: reserved `mark-ready-for-ci`, strict CI-candidate record/stage, and missing-adapter block until live implementation |
| Draft-to-ready succeeds remotely but crashes before local state records it, or review starts already ready | Plans 1 and 6: durable intent/challenge/finalize protocol, canonical transition subject, idempotency key, and verified already-ready no-op |
| Presentation can emit stale green after a concurrent finding commit | Plan 6: generation/state-byte binding plus immediate locked re-read and two-process barrier regression before proof emission |
| Receipt subject equality permits cross-review replay | Plans 1, 2, 4, and 6: verifier inputs/outputs bind and compare kind, review, dispatch, epoch, fingerprint, subject, and challenge for every kind |
| Duplicate JSON keys can erase findings or assignments before validation | Plan 1: one duplicate-key-rejecting decoder at every state/CLI/adapter/report/receipt/observation boundary and exact raw-byte completion binding |
| Discovery policy can qualify its own weakened traversal | Plans 1 and 2: externally/base-rooted policy identity/version/digest and reviewed-head self-qualification regression |
| Final or closure can return `not-applicable` while claiming clean | Plans 1, 4, and 6: role-specific result schemas requiring affirmative `covered` for both gates |
| Evidence ingestion action/caps/ACL policy are unspecified or caller-widenable | Plan 1: sealed source-identified ingestion policy and engine-derived action/candidate/source context with concrete cap/trustee/mode fields |
| A large synthetic kernel can land before production receipt custody is known feasible | Plan 1 Task 0: published live capability matrix is a hard prerequisite; missing routes block before kernel implementation |
| Metrics, encoding normalization, ledger rendering, and summaries consume semantic graph nodes | Plan 7: derived views behind the CLI; bookkeeping nodes removed |
| No objective test that weaker agents match frontier-reliable detection | Plan 7: blinded frontier-reference corpus and 100% per-trial recall/zero-false-green release gate |
| Expensive external frontier review can still reject internally green PRs for issues the workflow should have caught | Plan 7: blinded downstream frontier audit, zero-escape benchmark gate, missed-tier classification, and permanent escape fixtures |
| Current focused suite is red | Plan 1: preserve the intended lens-triage contract, make the minimal route correction, and require the complete focused suite green |

## Handoff notes

- The earlier `iterative-review-triage-and-selection` roadmap remains historical evidence for completed Plan 1. Its pending generation-aware selection and automated triage work is superseded by Plans 3 and 4 here.
- Plans 2-7 are intentionally written just-in-time after the previous plan lands. Their titles and acceptance boundaries are fixed by this roadmap, but implementation detail must use the then-current source.
- Plan 1 is sequential because its transition policy consumes the model and store produced by earlier tasks. Use `/executing-plans` for Plan 1; later plans may select `/subagent-driven-development` when their tasks are independent.
