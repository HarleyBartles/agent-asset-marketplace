# AGENTS.md

Scope: `.agents/runbooks/`

This scope covers deterministic operational procedures for agents.

Use runbooks when the work has a known ordered sequence, a fixed entrypoint,
explicit validation, and clear recovery steps.

Keep runbooks concrete. Put branching or scenario selection in a playbook.
