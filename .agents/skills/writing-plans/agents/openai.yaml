version: 1
metadata:
  skill_name: writing-plans
  source_category: first_party
  plugin: superpowers-plus
  projection_plugin: superpowers-plus
interface:
  display_name: Writing Plans
  short_description: Use when you have a spec or requirements for a multi-step task, before touching code
  default_prompt: Use /writing-plans to use when you have a spec or requirements for a multi-step task, before touching code
policy:
  products:
  - codex
  allow_implicit_invocation: true
