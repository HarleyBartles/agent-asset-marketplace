# AGENTS.md

Scope: `.agents/plugins/`

This scope covers the repo-local plugin posture surface consumed by Codex.

Defer to the repository root `AGENTS.md` for global repo doctrine and to
`.agents/docs/mesh-policy.md` for mesh rules.

This scope owns local plugin posture, not source custody. Keep
`.agents/plugins/marketplace.json` generated/checkable from repo inventory plus
policy, and keep navigation in generated `INDEX.md` files.
