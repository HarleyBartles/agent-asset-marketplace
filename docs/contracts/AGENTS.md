# AGENTS.md

Scope: `docs/contracts/`

This scope covers the docs-owned contract surfaces that define markdown and YAML
contracts for the marketplace and skill projections.

Defer to `../AGENTS.md` for the broader docs-routing context and to
`../../.agents/docs/mesh-policy.md` for mesh-specific law.

Keep this scope short. It exists to route agents to the right contract doc.

## Routing pointers

- `skill-frontmatter.md` for skill frontmatter and projection metadata contracts
- `openai-agent-yaml.md` for OpenAI agent YAML contract surfaces
- `../AGENTS.md` for broader docs-owned routing context
