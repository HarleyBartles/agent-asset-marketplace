# Canonical Plugin-Qualified Skill References Implementation Plan> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.**Goal:** Canonicalize cross-plugin marketplace skill references to `plugin_name:skill_name` and add validation that keeps those references from regressing.**Architecture:** Update only the source seams proven by the MARK-178 inventory. The source skill docs should name cross-plugin targets with their plugin-qualified identity, while GPT overlay/export prompt data stays untouched. Add a targeted validator test that enforces the known cross-plugin case without forbidding same-plugin bare references.**Tech Stack:** Markdown skill sources, Python marketplace validator, unittest.---### Task 1: Normalize the cross-plugin receipt reference**Files:**- Modify: `codex-marketplace/plugins/house-skills/skills/codex-receipts-superpowers/SKILL.md`- Modify: `codex-marketplace/plugins/superpowers/skills/codex-receipts-superpowers/SKILL.md`- Test: `tests/test_validate_marketplace.py`- [x] **Step 1: Write the failing test**Add a unit test that asserts the marketplace source copy for `codex-receipts-superpowers` contains the canonical `repo-worker-base:codex-repo-receipts` reference and does not rely on the bare cross-plugin form in source text.```pythondef test_codex_receipts_superpowers_uses_canonical_cross_plugin_reference(self) -> None:    source_text = (        ROOT        / "codex-marketplace"        / "plugins"        / "house-skills"        / "skills"        / "codex-receipts-superpowers"        / "SKILL.md"    ).read_text(encoding="utf-8")    self.assertIn("repo-worker-base:codex-repo-receipts", source_text)    self.assertNotIn("@codex-repo-receipts", source_text)```- [x] **Step 2: Run test to verify it fails**Run: `py -3 -m unittest tests.test_validate_marketplace.ValidateMarketplaceTests.test_codex_receipts_superpowers_uses_canonical_cross_plugin_reference -v`Expected: FAIL because the source still uses the bare cross-plugin form.- [x] **Step 3: Write minimal implementation**Change the source prose in both `codex-receipts-superpowers` copies so the cross-plugin artifact contract reference reads:```mdUse `repo-worker-base:codex-repo-receipts` for the exact artifact contract, default paths, and narrow omission rule.```Keep the GPT overlay/export prompt text in `agents/openai.yaml` unchanged.- [x] **Step 4: Run test to verify it passes**Run: `py -3 -m unittest tests.test_validate_marketplace.ValidateMarketplaceTests.test_codex_receipts_superpowers_uses_canonical_cross_plugin_reference -v`Expected: PASS.- [x] **Step 5: Commit**```bashgit add codex-marketplace/plugins/house-skills/skills/codex-receipts-superpowers/SKILL.md codex-marketplace/plugins/superpowers/skills/codex-receipts-superpowers/SKILL.md tests/test_validate_marketplace.pygit commit -m "fix: canonicalize cross-plugin receipt reference"```### Task 2: Add a targeted validation guard**Files:**- Modify: `tools/validate_marketplace.py`- Test: `tests/test_validate_marketplace.py`- [x] **Step 1: Write the failing test**Add a unit test that patches the repo root to a temp tree and verifies the validator rejects a source copy that still contains the bare cross-plugin `@codex-repo-receipts` reference.```pythondef test_validate_marketplace_rejects_bare_cross_plugin_receipt_reference(self) -> None:    with tempfile.TemporaryDirectory() as temp_dir:        temp_root = Path(temp_dir)        source_roots = (            temp_root / "codex-marketplace" / "plugins" / "house-skills" / "skills" / "codex-receipts-superpowers",            temp_root / "codex-marketplace" / "plugins" / "superpowers" / "skills" / "codex-receipts-superpowers",            temp_root / "sources" / "first_party" / "skills" / "house-skills",        )        for source_root in source_roots:            source_root.mkdir(parents=True, exist_ok=True)        _touch(source_roots[0] / "SKILL.md", "---\nname: codex-receipts-superpowers\n---\nUse `@codex-repo-receipts`.\n")        _touch(source_roots[1] / "SKILL.md", "---\nname: codex-receipts-superpowers\n---\nUse `repo-worker-base:codex-repo-receipts`.\n")        _touch(source_roots[2] / "decisions.md", "| MARK-162 | codex-receipts-superpowers-v1 | `codex-marketplace/plugins/house-skills/skills/codex-receipts-superpowers` | codex-receipts-superpowers | MARK-162 | imported | scope | Composes `repo-worker-base:codex-repo-receipts`. |\n")        _touch(source_roots[2] / "decisions.json", "[{\"notes\": \"Composes repo-worker-base:codex-repo-receipts.\"}]\n")        _touch(source_roots[2] / "intake.json", "[{\"notes\": \"Composes repo-worker-base:codex-repo-receipts.\"}]\n")        with patch("validate_marketplace.ROOT", temp_root):            with self.assertRaisesRegex(                ValueError,                r"codex-receipts-superpowers[\\/]+SKILL\\.md must use repo-worker-base:codex-repo-receipts",            ):                validate_marketplace.validate_canonical_cross_plugin_skill_references()```- [x] **Step 2: Run test to verify it fails**Run: `py -3 -m unittest tests.test_validate_marketplace.ValidateMarketplaceTests.test_validate_marketplace_rejects_bare_cross_plugin_receipt_reference -v`Expected: FAIL because the validator helper does not exist yet.- [x] **Step 3: Write minimal implementation**Add a small helper in `tools/validate_marketplace.py` that checks the known source surfaces for the canonical cross-plugin reference and raises if it finds the bare `@codex-repo-receipts` form in marketplace source text.- [x] **Step 4: Run test to verify it passes**Run: `py -3 -m unittest tests.test_validate_marketplace.ValidateMarketplaceTests.test_validate_marketplace_rejects_bare_cross_plugin_receipt_reference -v`Expected: PASS.- [x] **Step 5: Commit**```bashgit add tools/validate_marketplace.py tests/test_validate_marketplace.pygit commit -m "test: guard canonical cross-plugin skill references"```### Task 3: Validate the repo surface**Files:**- None- [x] **Step 1: Run marketplace validation**Run: `py -3 tools/validate_marketplace.py`Expected: PASS.- [x] **Step 2: Run diff hygiene**Run: `git diff --check`Expected: PASS.- [x] **Step 3: Regenerate generated skill zips**Run: `py -3 tools/update_skill_artifacts.py --all`Expected: PASS, with the regenerated `generated/skill-zips/` corpus matching the updated source references.- [x] **Step 4: Capture publication evidence**Record branch name, base SHA, final head SHA, validation output, and changed files for the issue closeout.
