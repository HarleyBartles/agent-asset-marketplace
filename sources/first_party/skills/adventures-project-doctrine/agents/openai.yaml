interface:
  display_name: Adventures Project Doctrine
  short_description: Shared Adventures of Patch repo, Patch, source-package, proof-run,
    and skill-routing doctrine.
  default_prompt: Use /adventures-project-doctrine when shared adventures doctrine
    for repo truth, source packages, bundle projection, and stage-boundary discipline
    across planning, source discovery, qa, image readiness, generation/editing, asset
    compilation, deck/package work, and acceptance.
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
