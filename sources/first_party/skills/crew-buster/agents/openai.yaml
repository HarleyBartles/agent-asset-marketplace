interface:
  display_name: Crew Buster
  short_description: Interrogate plans, route choices, and failure modes with the
    Crew lenses before action.
  default_prompt: Use $crew-buster to check route, authority, and fallback before
    action.
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
