interface:
  display_name: Adventures Bootstrap
  short_description: Bootstrap Adventures sessions and route requests.
  default_prompt: Use /adventures-bootstrap when bootstrap adventures of patch sessions
    by classifying project work, handoffs, deck, asset, issue, dispatch, receipt,
    skill-update, visual-production, and repo-sensitive requests before routing to
    required doctrine or task skills.
policy:
  products:
  - chatgpt
  - codex
  - api
  - atlas
  allow_implicit_invocation: true
